#!/usr/bin/env bash
# Local dev wrapper — delegates to gh-todo.ts in the same directory.
# Installed users get a wrapper generated by npm (via the bin field in package.json).
exec node --experimental-strip-types --no-warnings "$(dirname "$0")/gh-todo.ts" "$@"
